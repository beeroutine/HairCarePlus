<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:HairCarePlus.Client.Patient.Features.Calendar.Views"
             xmlns:viewmodels="clr-namespace:HairCarePlus.Client.Patient.Features.Calendar.ViewModels"
             x:Class="HairCarePlus.Client.Patient.Features.Calendar.Views.CalendarPage"
             Title="{Binding Title}">
    
    <ContentPage.Resources>
        <ResourceDictionary Source="../Resources/CalendarResources.xaml" />
    </ContentPage.Resources>
    
    <ContentPage.BindingContext>
        <viewmodels:CalendarViewModel />
    </ContentPage.BindingContext>
    
    <RefreshView IsRefreshing="{Binding IsBusy}">
        <ScrollView>
            <StackLayout Spacing="0">
                <!-- Active Restrictions section -->
                <Frame Style="{StaticResource CardStyle}" 
                       IsVisible="{Binding ActiveRestrictions, Converter={StaticResource HasItemsConverter}}">
                    <StackLayout Spacing="8">
                        <Label Text="Active Restrictions" 
                               Style="{StaticResource SubheaderStyle}" />
                               
                        <CollectionView ItemsSource="{Binding ActiveRestrictions}" 
                                        HeightRequest="120"
                                        SelectionMode="None">
                            <CollectionView.ItemsLayout>
                                <LinearItemsLayout Orientation="Horizontal" 
                                                   ItemSpacing="12" />
                            </CollectionView.ItemsLayout>
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Frame CornerRadius="8" 
                                           HasShadow="False" 
                                           BorderColor="#E0E0E0"
                                           BackgroundColor="White"
                                           WidthRequest="200">
                                        <Grid RowDefinitions="Auto,*,Auto" RowSpacing="8">
                                            <Label Grid.Row="0" 
                                                   Text="{Binding Title}" 
                                                   LineBreakMode="TailTruncation"
                                                   FontAttributes="Bold" />
                                                   
                                            <Label Grid.Row="1" 
                                                   Text="{Binding Description}" 
                                                   LineBreakMode="WordWrap"
                                                   MaxLines="2"
                                                   TextColor="{StaticResource TextSecondaryColor}" />
                                                   
                                            <Label Grid.Row="2" 
                                                   Text="{Binding ExpirationDate, StringFormat='Until {0:MMM dd}'}" 
                                                   FontAttributes="Bold"
                                                   TextColor="{StaticResource PrimaryColor}" />
                                        </Grid>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </StackLayout>
                </Frame>
                
                <!-- Horizontal Calendar Widget -->
                <Frame Style="{StaticResource CardStyle}">
                    <views:HorizontalCalendarView BindingContext="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:CalendarViewModel}}}" />
                </Frame>
                
                <!-- Monthly Calendar -->
                <Frame Style="{StaticResource CardStyle}">
                    <views:MonthCalendarView SelectedDate="{Binding SelectedDate}" 
                                             BindingContext="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:CalendarViewModel}}}" />
                </Frame>
                
                <!-- Daily Tasks -->
                <Frame Style="{StaticResource CardStyle}" IsVisible="{Binding EventsForSelectedDate, Converter={StaticResource HasItemsConverter}}">
                    <StackLayout Spacing="8">
                        <Label Text="{Binding SelectedDate, StringFormat='Tasks for {0:MMMM d, yyyy}'}" 
                               Style="{StaticResource SubheaderStyle}" />
                               
                        <CollectionView ItemsSource="{Binding EventsForSelectedDate}" 
                                        SelectionMode="None">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Frame Margin="0,4" 
                                           Padding="16" 
                                           CornerRadius="8" 
                                           BorderColor="#E0E0E0">
                                        <Grid ColumnDefinitions="Auto,*,Auto" RowDefinitions="Auto,Auto">
                                            <!-- Event type indicator -->
                                            <BoxView Grid.Row="0" Grid.RowSpan="2" Grid.Column="0"
                                                     WidthRequest="4"
                                                     Margin="0,0,12,0"
                                                     BackgroundColor="{Binding EventType, Converter={StaticResource EventTypeToColorConverter}}" />
                                                     
                                            <!-- Event title -->
                                            <Label Grid.Row="0" Grid.Column="1"
                                                   Text="{Binding Title}"
                                                   FontAttributes="Bold"
                                                   TextColor="{StaticResource TextPrimaryColor}"
                                                   Margin="0,0,0,4" />
                                                   
                                            <!-- Event description -->
                                            <Label Grid.Row="1" Grid.Column="1"
                                                   Text="{Binding Description}"
                                                   TextColor="{StaticResource TextSecondaryColor}"
                                                   LineBreakMode="WordWrap" />
                                                   
                                            <!-- Completion checkbox -->
                                            <CheckBox Grid.Row="0" Grid.Column="2"
                                                      IsChecked="{Binding IsCompleted}"
                                                      Color="{StaticResource PrimaryColor}">
                                                <CheckBox.Triggers>
                                                    <DataTrigger TargetType="CheckBox"
                                                                 Binding="{Binding EventType}"
                                                                 Value="Restriction">
                                                        <Setter Property="IsVisible" Value="False" />
                                                    </DataTrigger>
                                                </CheckBox.Triggers>
                                            </CheckBox>
                                        </Grid>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </StackLayout>
                </Frame>
            </StackLayout>
        </ScrollView>
    </RefreshView>
</ContentPage> 