<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:HairCarePlus.Client.Patient.Features.Calendar.Views"
             xmlns:helpers="clr-namespace:HairCarePlus.Client.Patient.Features.Calendar.Helpers"
             xmlns:models="clr-namespace:HairCarePlus.Client.Patient.Features.Calendar.Models"
             x:Class="HairCarePlus.Client.Patient.Features.Calendar.Views.CalendarPage"
             Title="Календарь">
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <!-- Конвертеры -->
            <helpers:BooleanInvertConverter x:Key="BoolInvertConverter" />
            
            <!-- Минимально необходимые стили -->
            <Style x:Key="BasicLabelStyle" TargetType="Label">
                <Setter Property="FontSize" Value="16" />
                <Setter Property="TextColor" Value="Black" />
            </Style>
            
            <Style x:Key="HeaderLabelStyle" TargetType="Label">
                <Setter Property="FontSize" Value="20" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="TextColor" Value="Black" />
                <Setter Property="HorizontalOptions" Value="Center" />
            </Style>
            
            <Style x:Key="BasicButtonStyle" TargetType="Button">
                <Setter Property="BackgroundColor" Value="#6200EE" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="Margin" Value="5" />
                <Setter Property="CornerRadius" Value="5" />
            </Style>
            
            <!-- Стили для дней календаря -->
            <Style x:Key="CalendarDayStyle" TargetType="Button">
                <Setter Property="WidthRequest" Value="40" />
                <Setter Property="HeightRequest" Value="40" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderColor" Value="Transparent" />
                <Setter Property="BorderWidth" Value="0" />
                <Setter Property="TextColor" Value="Black" />
                <Setter Property="Margin" Value="2" />
            </Style>
            
            <Style x:Key="CalendarDayOfWeekStyle" TargetType="Label">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="TextColor" Value="#666666" />
                <Setter Property="HorizontalOptions" Value="Center" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*,Auto">
        <!-- Заголовок с названием текущего месяца -->
        <Grid Grid.Row="0" 
              ColumnDefinitions="Auto,*,Auto" 
              Padding="10" 
              BackgroundColor="#F5F5F5">
            
            <Button Grid.Column="0" 
                    Text="&lt;" 
                    Command="{Binding PreviousMonthCommand}"
                    Style="{StaticResource BasicButtonStyle}" />
            
            <Label Grid.Column="1" 
                   Text="{Binding CurrentMonthYear}" 
                   Style="{StaticResource HeaderLabelStyle}" 
                   VerticalOptions="Center" />
            
            <Button Grid.Column="2" 
                    Text="&gt;" 
                    Command="{Binding NextMonthCommand}"
                    Style="{StaticResource BasicButtonStyle}" />
        </Grid>
        
        <!-- Основное содержимое -->
        <VerticalStackLayout Grid.Row="1" Padding="10" Spacing="10">
            <!-- Показываем когда IsMonthViewVisible = false -->
            <VerticalStackLayout IsVisible="{Binding IsMonthViewVisible, Converter={StaticResource BoolInvertConverter}}">
                <Label Text="{Binding SelectedDateText}" 
                       Style="{StaticResource HeaderLabelStyle}" />
                
                <Label Text="События на этот день:" 
                       Style="{StaticResource BasicLabelStyle}" 
                       Margin="0,10,0,5" />
                
                <Label Text="Нет событий на выбранный день" 
                       IsVisible="{Binding HasSelectedDayEvents, Converter={StaticResource BoolInvertConverter}}"
                       Style="{StaticResource BasicLabelStyle}" 
                       Opacity="0.7" />
                
                <!-- Список событий на выбранный день -->
                <CollectionView ItemsSource="{Binding SelectedDayEvents}"
                                IsVisible="{Binding HasSelectedDayEvents}"
                                Margin="0,5,0,10">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:CalendarEvent">
                            <Frame Margin="0,5" Padding="10" BorderColor="#E0E0E0">
                                <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto,Auto">
                                    <!-- Индикатор типа события -->
                                    <BoxView Grid.RowSpan="3" 
                                             WidthRequest="4" 
                                             BackgroundColor="#6200EE" 
                                             Margin="0,0,10,0" />
                                    
                                    <!-- Название события -->
                                    <Label Grid.Column="1" 
                                           Grid.Row="0" 
                                           Text="{Binding Title}" 
                                           FontSize="16" 
                                           FontAttributes="Bold" />
                                    
                                    <!-- Описание события -->
                                    <Label Grid.Column="1" 
                                           Grid.Row="1" 
                                           Text="{Binding Description}" 
                                           FontSize="14" 
                                           Margin="0,5,0,5" />
                                    
                                    <!-- Время события (если есть) -->
                                    <Label Grid.Column="1" 
                                           Grid.Row="2" 
                                           Text="08:00" 
                                           FontSize="12" 
                                           TextColor="#666666" />
                                </Grid>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
                
                <Button Text="Вернуться к месяцу" 
                        Command="{Binding BackToMonthViewCommand}"
                        Style="{StaticResource BasicButtonStyle}" 
                        Margin="0,10,0,0" />
            </VerticalStackLayout>
            
            <!-- Показываем когда IsMonthViewVisible = true - вид календаря -->
            <VerticalStackLayout IsVisible="{Binding IsMonthViewVisible}" Spacing="5">
                <!-- Дни недели -->
                <Grid ColumnDefinitions="*,*,*,*,*,*,*">
                    <Label Grid.Column="0" Text="ПН" Style="{StaticResource CalendarDayOfWeekStyle}" />
                    <Label Grid.Column="1" Text="ВТ" Style="{StaticResource CalendarDayOfWeekStyle}" />
                    <Label Grid.Column="2" Text="СР" Style="{StaticResource CalendarDayOfWeekStyle}" />
                    <Label Grid.Column="3" Text="ЧТ" Style="{StaticResource CalendarDayOfWeekStyle}" />
                    <Label Grid.Column="4" Text="ПТ" Style="{StaticResource CalendarDayOfWeekStyle}" />
                    <Label Grid.Column="5" Text="СБ" Style="{StaticResource CalendarDayOfWeekStyle}" />
                    <Label Grid.Column="6" Text="ВС" Style="{StaticResource CalendarDayOfWeekStyle}" />
                </Grid>
                
                <!-- Простая статическая сетка календаря как заглушка -->
                <Grid ColumnDefinitions="*,*,*,*,*,*,*" 
                      RowDefinitions="*,*,*,*,*,*" 
                      Margin="0,5,0,0">
                    
                    <!-- Первая неделя (пустые дни и начало месяца) -->
                    <Label Grid.Row="0" Grid.Column="0" Text="" Style="{StaticResource CalendarDayStyle}" />
                    <Label Grid.Row="0" Grid.Column="1" Text="" Style="{StaticResource CalendarDayStyle}" />
                    <Label Grid.Row="0" Grid.Column="2" Text="" Style="{StaticResource CalendarDayStyle}" />
                    <Label Grid.Row="0" Grid.Column="3" Text="" Style="{StaticResource CalendarDayStyle}" />
                    <Button Grid.Row="0" Grid.Column="4" Text="1" Style="{StaticResource CalendarDayStyle}" Command="{Binding DaySelectedCommand}" CommandParameter="1" />
                    <Button Grid.Row="0" Grid.Column="5" Text="2" Style="{StaticResource CalendarDayStyle}" Command="{Binding DaySelectedCommand}" CommandParameter="2" />
                    <Button Grid.Row="0" Grid.Column="6" Text="3" Style="{StaticResource CalendarDayStyle}" Command="{Binding DaySelectedCommand}" CommandParameter="3" />
                    
                    <!-- Вторая неделя -->
                    <Button Grid.Row="1" Grid.Column="0" Text="4" Style="{StaticResource CalendarDayStyle}" Command="{Binding DaySelectedCommand}" CommandParameter="4" />
                    <Button Grid.Row="1" Grid.Column="1" Text="5" Style="{StaticResource CalendarDayStyle}" Command="{Binding DaySelectedCommand}" CommandParameter="5" />
                    <Button Grid.Row="1" Grid.Column="2" Text="6" Style="{StaticResource CalendarDayStyle}" Command="{Binding DaySelectedCommand}" CommandParameter="6" />
                    <Button Grid.Row="1" Grid.Column="3" Text="7" Style="{StaticResource CalendarDayStyle}" Command="{Binding DaySelectedCommand}" CommandParameter="7" />
                    <Button Grid.Row="1" Grid.Column="4" Text="8" Style="{StaticResource CalendarDayStyle}" Command="{Binding DaySelectedCommand}" CommandParameter="8" />
                    <Button Grid.Row="1" Grid.Column="5" Text="9" Style="{StaticResource CalendarDayStyle}" Command="{Binding DaySelectedCommand}" CommandParameter="9" />
                    <Button Grid.Row="1" Grid.Column="6" Text="10" Style="{StaticResource CalendarDayStyle}" Command="{Binding DaySelectedCommand}" CommandParameter="10" />
                    
                    <!-- Остальные недели ... -->
                </Grid>
                
                <!-- Кнопка перехода к текущей дате -->
                <Button Text="Перейти к сегодня" 
                        Command="{Binding GoToTodayCommand}"
                        Style="{StaticResource BasicButtonStyle}" 
                        HorizontalOptions="Center" 
                        Margin="0,10,0,0" />
            </VerticalStackLayout>
        </VerticalStackLayout>
        
        <!-- Кнопка для отладки -->
        <Button Grid.Row="2"
                Text="Диагностика" 
                Clicked="DiagnosticButton_Clicked"
                Style="{StaticResource BasicButtonStyle}"
                HorizontalOptions="Center" 
                Margin="0,10,0,10" />
    </Grid>
</ContentPage> 