<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:HairCarePlus.Client.Patient.Features.Calendar.ViewModels"
             xmlns:models="clr-namespace:HairCarePlus.Client.Patient.Features.Calendar.Models"
             xmlns:converters="clr-namespace:HairCarePlus.Client.Patient.Features.Calendar.Converters"
             xmlns:system="clr-namespace:System;assembly=netstandard"
             x:Class="HairCarePlus.Client.Patient.Features.Calendar.Views.TodayPage"
             x:DataType="viewmodels:TodayViewModel"
             BackgroundColor="{AppThemeBinding Light=#F7F7F7, Dark=#121212}"
             Title="Today">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:EventIndicatorsConverter x:Key="EventIndicatorsConverter"/>
            <converters:DateHasEventTypeConverter x:Key="DateHasEventTypeConverter"/>
            <converters:DateHasAnyEventConverter x:Key="DateHasAnyEventConverter"/>
            <converters:DateHasAnyEventColorConverter x:Key="DateHasAnyEventColorConverter"/>
            <converters:TimeOfDayToStringConverter x:Key="TimeOfDayToStringConverter"/>
            <converters:TimeOfDayToColorConverter x:Key="TimeOfDayToColorConverter"/>
            <converters:IsExpiredConverter x:Key="IsExpiredConverter"/>
            <converters:EventPriorityToColorConverter x:Key="EventPriorityToColorConverter"/>
            <converters:EventPriorityToIconConverter x:Key="EventPriorityToIconConverter"/>
            <converters:PriorityVisibilityConverter x:Key="PriorityVisibilityConverter"/>
            <converters:BoolToColorConverter x:Key="BoolToColorConverter"/>
            <converters:ProgressToAngleConverter x:Key="ProgressToAngleConverter"/>
            <x:Double x:Key="CircleSize">100</x:Double>
            
            <!-- Layout Resources -->
            <LinearItemsLayout x:Key="HorizontalLinearLayout" 
                             Orientation="Horizontal" 
                             ItemSpacing="5"
                             SnapPointsType="MandatorySingle"
                             SnapPointsAlignment="Center"/>
            
            <!-- Date Selector Item Template -->
            <DataTemplate x:Key="DateTemplate">
                <Grid x:Name="rootContainer" WidthRequest="55" HeightRequest="55" Padding="0" Margin="3,0">
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroup Name="CommonStates">
                            <VisualState Name="Normal" />
                            <VisualState Name="Selected">
                                <VisualState.Setters>
                                    <Setter Property="Frame.BackgroundColor" 
                                            TargetName="dayFrame" 
                                            Value="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}" />
                                    <Setter Property="Frame.BorderColor" 
                                            TargetName="dayFrame" 
                                            Value="Transparent" />
                                    <Setter Property="Label.TextColor" 
                                            TargetName="dayText" 
                                            Value="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Black}}" />
                                    <Setter Property="Grid.Scale" Value="1.05" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateManager.VisualStateGroups>
                    
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Day of Week -->
                    <Label Grid.Row="0" 
                           Text="{Binding ., StringFormat='{0:ddd}'}"
                           FontSize="12"
                           TextColor="{StaticResource Gray500}"
                           HorizontalOptions="Center"
                           Margin="0,0,0,2"/>

                    <!-- Day number in circle -->
                    <Frame Grid.Row="1"
                           x:Name="dayFrame"
                           CornerRadius="20"
                           Padding="0"
                           WidthRequest="40"
                           HeightRequest="40"
                           BackgroundColor="{Binding ., Converter={StaticResource DateHasAnyEventColorConverter}}"
                           BorderColor="Transparent"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"
                           Shadow="{Binding ., Converter={StaticResource DateHasAnyEventConverter}, ConverterParameter=Shadow}">
                        <Label x:Name="dayText"
                               Text="{Binding ., StringFormat='{0:%d}'}"
                               FontSize="16"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"
                               TextColor="{Binding ., Converter={StaticResource DateHasAnyEventConverter}, ConverterParameter=TextColor}"/>
                    </Frame>

                    <!-- Event Indicators bar -->
                    <BoxView Grid.Row="2"
                             HeightRequest="3"
                             CornerRadius="1.5"
                             WidthRequest="24"
                             HorizontalOptions="Center"
                             Margin="0,4,0,0"
                             IsVisible="{Binding ., Converter={StaticResource DateHasAnyEventConverter}}"
                             Color="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}" />

                    <!-- Event Indicators details -->
                    <HorizontalStackLayout Grid.Row="2"
                                         VerticalOptions="End" 
                                         HorizontalOptions="Center"
                                         Spacing="3"
                                         Margin="0,10,0,0">
                        <BoxView WidthRequest="6" 
                                HeightRequest="6" 
                                CornerRadius="3"
                                Color="{StaticResource MedicationBlue}"
                                IsVisible="{Binding ., Converter={StaticResource DateHasEventTypeConverter}, ConverterParameter={x:Static models:EventType.MedicationTreatment}}"/>
                        
                        <BoxView WidthRequest="6" 
                                HeightRequest="6"
                                CornerRadius="3"
                                Color="{StaticResource PhotoPurple}"
                                IsVisible="{Binding ., Converter={StaticResource DateHasEventTypeConverter}, ConverterParameter={x:Static models:EventType.Photo}}"/>
                                
                        <BoxView WidthRequest="6" 
                                HeightRequest="6"
                                CornerRadius="3"
                                Color="{StaticResource VideoOrange}"
                                IsVisible="{Binding ., Converter={StaticResource DateHasEventTypeConverter}, ConverterParameter={x:Static models:EventType.Video}}"/>
                    </HorizontalStackLayout>
                    
                    <Grid.GestureRecognizers>
                        <TapGestureRecognizer 
                            Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:TodayViewModel}}, Path=SelectDateCommand}"
                            CommandParameter="{Binding .}" />
                        <TapGestureRecognizer 
                            Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:TodayViewModel}}, Path=OpenMonthCalendarCommand}"
                            CommandParameter="{Binding .}"
                            NumberOfTapsRequired="2" />
                    </Grid.GestureRecognizers>
                </Grid>
            </DataTemplate>

            <!-- Event Item Template -->
            <DataTemplate x:Key="EventTemplate" x:DataType="models:CalendarEvent">
                <Frame Margin="0,4,0,0" 
                       Padding="12"
                       CornerRadius="18"
                       BorderColor="Transparent"
                       BackgroundColor="{AppThemeBinding Light=#EAF4FC, Dark=#1E2A35}"
                       HasShadow="True"
                       Shadow="{OnPlatform iOS={StaticResource SmallShadow}}">
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                        <!-- Icon with background -->
                        <Frame Grid.Column="0"
                               WidthRequest="40"
                               HeightRequest="40"
                               CornerRadius="20"
                               Padding="0"
                               HasShadow="False"
                               BorderColor="Transparent"
                               BackgroundColor="{Binding EventType, Converter={StaticResource EventTypeToColorConverter}}"
                               HorizontalOptions="Center"
                               VerticalOptions="Start">
                            <Label Text="{Binding EventType, Converter={StaticResource EventTypeToIconConverter}}"
                                   FontFamily="MaterialIcons"
                                   FontSize="20"
                                   TextColor="White"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center"/>
                        </Frame>

                        <!-- Task Content -->
                        <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Start">
                            <Label Text="{Binding Date, StringFormat='{0:HH:mm}'}"
                                   FontSize="14"
                                   FontAttributes="Bold"
                                   Margin="0,0,0,2"
                                   TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"/>
                            <Label Text="{Binding Title}"
                                   FontSize="16"
                                   FontAttributes="None"
                                   TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"/>
                            <Grid>
                                <Label x:Name="descriptionLabel"
                                       Text="{Binding Description}"
                                       FontSize="14"
                                       LineBreakMode="TailTruncation"
                                       MaxLines="2"
                                       TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"/>
                                <Label Text="Подробнее..."
                                       FontSize="13"
                                       TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"
                                       HorizontalOptions="End"
                                       VerticalOptions="End"
                                       IsVisible="{Binding Source={x:Reference descriptionLabel}, Path=LineBreakMode, Converter={StaticResource StringNotEmptyConverter}}">
                                    <Label.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:TodayViewModel}}, Path=ShowEventDetailsCommand}"
                                                             CommandParameter="{Binding .}"/>
                                    </Label.GestureRecognizers>
                                </Label>
                            </Grid>
                        </VerticalStackLayout>
                    </Grid>
                </Frame>
            </DataTemplate>
            
            <!-- No events template -->
            <DataTemplate x:Key="EmptyViewTemplate">
                <StackLayout VerticalOptions="CenterAndExpand" 
                             HorizontalOptions="CenterAndExpand" 
                             Padding="20">
                    <Image Source="empty_calendar.png"
                           WidthRequest="60"
                           HeightRequest="60"
                           Opacity="0.6"
                           IsVisible="false" />
                    <Label Text="No events scheduled for this day" 
                           HorizontalTextAlignment="Center"
                           FontSize="16"
                           Margin="0,20,0,0"
                           TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}"/>
                </StackLayout>
            </DataTemplate>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*" Padding="0">
        <!-- Fixed Header -->
        <Frame Grid.Row="0" 
               BackgroundColor="{AppThemeBinding Light=#F7F7F7, Dark=#121212}"
               BorderColor="Transparent"
               CornerRadius="0"
               Margin="0"
               Padding="0"
               HasShadow="{OnPlatform iOS=True, Default=False}">
            <Grid Padding="16,8,16,12">
                <VerticalStackLayout Spacing="12">
                    <!-- Updated Header Design with Day Circle and Month -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="15" Margin="0,16,0,8">
                        <!-- Center: Circular Progress with Day Number and Day Name -->
                        <Grid Grid.Column="0"
                              HorizontalOptions="Start"
                              VerticalOptions="Center"
                              WidthRequest="{StaticResource CircleSize}"
                              HeightRequest="{StaticResource CircleSize}">
                              
                            <!-- Background Circle -->
                            <Border StrokeShape="Ellipse"
                                   Stroke="{AppThemeBinding Light={StaticResource Gray300}, Dark={StaticResource Gray700}}"
                                   StrokeThickness="4"
                                   BackgroundColor="Transparent"
                                   WidthRequest="{StaticResource CircleSize}"
                                   HeightRequest="{StaticResource CircleSize}"/>
                                   
                            <!-- Progress Circle - colored border with opacity -->
                            <Frame BackgroundColor="Transparent"
                                   BorderColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource White}}"
                                   CornerRadius="50"
                                   HasShadow="False"
                                   Padding="0"
                                   WidthRequest="{StaticResource CircleSize}"
                                   HeightRequest="{StaticResource CircleSize}"
                                   Opacity="{Binding CompletionProgress}"/>
                            
                            <!-- Center Content -->
                            <VerticalStackLayout HorizontalOptions="Center" 
                                               VerticalOptions="Center"
                                               Spacing="0">
                                <!-- Day number (large) -->
                                <Label Text="{Binding Source={x:Static system:DateTime.Today}, StringFormat='{0:%d}'}"
                                       FontSize="40"
                                       FontAttributes="Bold"
                                       HorizontalOptions="Center"
                                       TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"/>
                                
                                <!-- Day of week (smaller) -->
                                <Label Text="{Binding Source={x:Static system:DateTime.Today}, StringFormat='{0:ddd}'}"
                                       FontSize="18"
                                       HorizontalOptions="Center"
                                       TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"/>
                            </VerticalStackLayout>
                            
                            <!-- Add Tap Gesture Recognizer -->
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding GoToTodayCommand}"/>
                            </Grid.GestureRecognizers>
                        </Grid>
                        
                        <!-- Right side: Month and transplant day info -->
                        <VerticalStackLayout Grid.Column="1" Spacing="4" VerticalOptions="Center" HorizontalOptions="End">
                            <Label Text="{Binding CurrentMonthName}"
                                   FontSize="34"
                                   FontAttributes="Bold"
                                   VerticalOptions="Center"
                                   HorizontalOptions="End"
                                   TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"/>
                                   
                            <Label Text="{Binding DaysSinceTransplant}" 
                                   FontSize="14"
                                   HorizontalOptions="End"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"/>
                                    
                            <!-- Diagnostic button -->
                            <Button Text="⚙️ Diagnostic Stats" 
                                    Command="{Binding ShowDiagnosticsCommand}"
                                    BackgroundColor="Transparent"
                                    TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource White}}"
                                    FontSize="12"
                                    Padding="8,2"
                                    HorizontalOptions="End"
                                    Margin="0,8,0,0"/>
                        </VerticalStackLayout>
                    </Grid>

                    <!-- Horizontal Day Ticker -->
                    <ScrollView x:Name="daysScrollView" 
                              HorizontalScrollBarVisibility="Never" 
                              VerticalScrollBarVisibility="Never"
                              Orientation="Horizontal">
                        <CollectionView x:Name="daysCollection"
                                      ItemsSource="{Binding CalendarDays}"
                                      SelectionChangedCommand="{Binding SelectDateCommand}"
                                      SelectionChangedCommandParameter="{Binding SelectedItem, Source={x:Reference daysCollection}}"
                                      SelectionMode="Single"
                                      HeightRequest="80"
                                      Margin="0,4,0,0"
                                      ItemSizingStrategy="MeasureFirstItem"
                                      ItemsUpdatingScrollMode="KeepLastItemInView"
                                      RemainingItemsThreshold="5"
                                      RemainingItemsThresholdReachedCommand="{Binding LoadMoreDatesCommand}"
                                      ItemsLayout="{StaticResource HorizontalLinearLayout}">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Grid x:Name="rootContainer" WidthRequest="55" HeightRequest="55" Padding="0" Margin="3,0">
                                        <VisualStateManager.VisualStateGroups>
                                            <VisualStateGroup Name="CommonStates">
                                                <VisualState Name="Normal" />
                                                <VisualState Name="Selected">
                                                    <VisualState.Setters>
                                                        <Setter Property="Frame.BackgroundColor" 
                                                                TargetName="dayFrame" 
                                                                Value="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}" />
                                                        <Setter Property="Frame.BorderColor" 
                                                                TargetName="dayFrame" 
                                                                Value="Transparent" />
                                                        <Setter Property="Label.TextColor" 
                                                                TargetName="dayText" 
                                                                Value="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Black}}" />
                                                        <Setter Property="Grid.Scale" Value="1.05" />
                                                    </VisualState.Setters>
                                                </VisualState>
                                            </VisualStateGroup>
                                        </VisualStateManager.VisualStateGroups>
                                        
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- Day of Week -->
                                        <Label Grid.Row="0" 
                                               Text="{Binding ., StringFormat='{0:ddd}'}"
                                               FontSize="12"
                                               TextColor="{StaticResource Gray500}"
                                               HorizontalOptions="Center"
                                               Margin="0,0,0,2"/>

                                        <!-- Day number in circle -->
                                        <Frame Grid.Row="1"
                                               x:Name="dayFrame"
                                               CornerRadius="20"
                                               Padding="0"
                                               WidthRequest="40"
                                               HeightRequest="40"
                                               BackgroundColor="{Binding ., Converter={StaticResource DateHasAnyEventColorConverter}}"
                                               BorderColor="Transparent"
                                               HorizontalOptions="Center"
                                               VerticalOptions="Center"
                                               Shadow="{Binding ., Converter={StaticResource DateHasAnyEventConverter}, ConverterParameter=Shadow}">
                                            <Label x:Name="dayText"
                                                   Text="{Binding ., StringFormat='{0:%d}'}"
                                                   FontSize="16"
                                                   HorizontalOptions="Center"
                                                   VerticalOptions="Center"
                                                   TextColor="{Binding ., Converter={StaticResource DateHasAnyEventConverter}, ConverterParameter=TextColor}"/>
                                        </Frame>

                                        <!-- Event Indicators bar -->
                                        <BoxView Grid.Row="2"
                                                 HeightRequest="3"
                                                 CornerRadius="1.5"
                                                 WidthRequest="24"
                                                 HorizontalOptions="Center"
                                                 Margin="0,4,0,0"
                                                 IsVisible="{Binding ., Converter={StaticResource DateHasAnyEventConverter}}"
                                                 Color="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}" />

                                        <!-- Event Indicators details -->
                                        <HorizontalStackLayout Grid.Row="2"
                                                             VerticalOptions="End" 
                                                             HorizontalOptions="Center"
                                                             Spacing="3"
                                                             Margin="0,10,0,0">
                                            <BoxView WidthRequest="6" 
                                                    HeightRequest="6" 
                                                    CornerRadius="3"
                                                    Color="{StaticResource MedicationBlue}"
                                                    IsVisible="{Binding ., Converter={StaticResource DateHasEventTypeConverter}, ConverterParameter={x:Static models:EventType.MedicationTreatment}}"/>
                                            
                                            <BoxView WidthRequest="6" 
                                                    HeightRequest="6"
                                                    CornerRadius="3"
                                                    Color="{StaticResource PhotoPurple}"
                                                    IsVisible="{Binding ., Converter={StaticResource DateHasEventTypeConverter}, ConverterParameter={x:Static models:EventType.Photo}}"/>
                                                    
                                            <BoxView WidthRequest="6" 
                                                    HeightRequest="6"
                                                    CornerRadius="3"
                                                    Color="{StaticResource VideoOrange}"
                                                    IsVisible="{Binding ., Converter={StaticResource DateHasEventTypeConverter}, ConverterParameter={x:Static models:EventType.Video}}"/>
                                        </HorizontalStackLayout>
                                        
                                        <Grid.GestureRecognizers>
                                            <TapGestureRecognizer 
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:TodayViewModel}}, Path=SelectDateCommand}"
                                                CommandParameter="{Binding .}" />
                                            <TapGestureRecognizer 
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:TodayViewModel}}, Path=OpenMonthCalendarCommand}"
                                                CommandParameter="{Binding .}"
                                                NumberOfTapsRequired="2" />
                                        </Grid.GestureRecognizers>
                                    </Grid>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </ScrollView>
                </VerticalStackLayout>
            </Grid>
        </Frame>

        <!-- Loading Indicator -->
        <Grid Grid.Row="0" 
              IsVisible="{Binding IsLoading}"
              BackgroundColor="#80000000"
              InputTransparent="False">
            <Border StrokeShape="RoundRectangle 10"
                    BackgroundColor="{StaticResource White}"
                    Padding="20"
                    HorizontalOptions="Center"
                    VerticalOptions="Center">
                <VerticalStackLayout Spacing="10">
                    <ActivityIndicator IsRunning="{Binding IsLoading}"
                                     Color="{StaticResource Primary}"
                                     HeightRequest="50"
                                     WidthRequest="50"/>
                    <Label Text="{Binding LoadingStatus}"
                           TextColor="{StaticResource Primary}"
                           HorizontalOptions="Center"/>
                    <ProgressBar Progress="{Binding LoadingProgress, Converter={StaticResource ProgressConverter}}"
                               ProgressColor="{StaticResource Primary}"
                               HeightRequest="6"
                               WidthRequest="200"/>
                    <Label Text="{Binding LoadingProgress, StringFormat='{0}%'}"
                           TextColor="{StaticResource Primary}"
                           HorizontalOptions="Center"
                           FontSize="12"/>
                </VerticalStackLayout>
            </Border>
        </Grid>

        <!-- Error State -->
        <Grid Grid.Row="0" 
              IsVisible="{Binding HasError}"
              BackgroundColor="#80000000"
              InputTransparent="False">
            <Frame BackgroundColor="{StaticResource White}"
                   CornerRadius="10"
                   Padding="20"
                   HorizontalOptions="Center"
                   VerticalOptions="Center">
                <VerticalStackLayout Spacing="10">
                    <Label Text="{Binding LoadingStatus}"
                           TextColor="{StaticResource Error}"
                           HorizontalOptions="Center"/>
                    <Button Text="Retry"
                            Command="{Binding LoadMoreDatesCommand}"
                            Style="{StaticResource PrimaryButton}"
                            HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </Frame>
        </Grid>

        <!-- Scrollable Content -->
        <ScrollView x:Name="EventsScrollView"
                  Grid.Row="1"
                  IsVisible="{Binding IsContentVisible}"
                  HorizontalScrollBarVisibility="Never"
                  VerticalScrollBarVisibility="Never"
                  BackgroundColor="{AppThemeBinding Light=#F7F7F7, Dark=#121212}">
            <RefreshView IsRefreshing="{Binding IsRefreshing}"
                       Command="{Binding RefreshCommand}">
                <CollectionView ItemsSource="{Binding SortedEvents}"
                              SelectionMode="None"
                              ItemsUpdatingScrollMode="KeepLastItemInView"
                              ItemTemplate="{StaticResource EventTemplate}"
                              EmptyView="{StaticResource EmptyViewTemplate}"
                              ItemSizingStrategy="MeasureFirstItem"
                              Margin="16,8,16,16">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Vertical" ItemSpacing="8"/>
                    </CollectionView.ItemsLayout>
                </CollectionView>
            </RefreshView>
        </ScrollView>
    </Grid>
</ContentPage> 