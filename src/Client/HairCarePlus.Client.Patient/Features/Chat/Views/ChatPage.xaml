<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:commonConverters="clr-namespace:HairCarePlus.Client.Patient.Common.Converters"
             xmlns:effects="clr-namespace:HairCarePlus.Client.Patient.Effects"
             xmlns:behaviors="clr-namespace:HairCarePlus.Client.Patient.Common.Behaviors"
             xmlns:chatBehaviors="clr-namespace:HairCarePlus.Client.Patient.Features.Chat.Behaviors"
             xmlns:entities="clr-namespace:HairCarePlus.Client.Patient.Features.Chat.Domain.Entities"
             x:Class="HairCarePlus.Client.Patient.Features.Chat.Views.ChatPage"
             BackgroundColor="{AppThemeBinding Light=#F7F7F7, Dark=#121212}"
             Shell.TabBarIsVisible="False"
             NavigationPage.HasNavigationBar="False">

    <ContentPage.Resources>
        <ResourceDictionary>
            <commonConverters:IsNotNullConverter x:Key="IsNotNullConverter" />
            <commonConverters:StringNotEmptyConverter x:Key="StringNotEmptyConverter" />
            <commonConverters:BoolToTextConverter x:Key="BoolToTextConverter" />
            <commonConverters:EqualityConverter x:Key="EqualityConverter" />
            
            <!-- Removed old Border styles (SelectedMessageFrame, MessageFrameStyle) as alignment now handled via DataTriggers -->
        </ResourceDictionary>
    </ContentPage.Resources>

    <!-- Custom in-page header (replaces Shell.TitleView for immersive mode) -->
    <Grid RowDefinitions="*,Auto">
        <!-- Header (overlay) -->
        <Grid
              ColumnDefinitions="44,*,Auto"
              Padding="20,20,20,8">
            <!-- Back button -->
            <Button Grid.Column="0"
                    Text="←"
                    Command="{Binding BackCommand}"
                    BackgroundColor="Transparent"
                    TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"
                    FontSize="24"
                    FontAttributes="Bold"
                    Padding="0"
                    HeightRequest="44"
                    WidthRequest="44"
                    HorizontalOptions="Start"/>

            <!-- Doctor name -->
            <Label Grid.Column="1"
                   Text="{Binding Doctor.Name}"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"
                   FontAttributes="Bold"
                   FontSize="18"
                   TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"/>

            <!-- Online status -->
            <Label Grid.Column="2"
                   Text="{Binding Doctor.IsOnline, Converter={StaticResource BoolToTextConverter}, ConverterParameter='Online,Offline'}"
                   HorizontalOptions="End"
                   VerticalOptions="Center"
                   FontSize="15"
                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray300}}"
                   Margin="0,0,0,0"/>
        </Grid>

        <!-- Messages -->
        <CollectionView Grid.Row="0"
                       x:Name="MessagesCollection"
                       ItemsSource="{Binding Messages}"
                       ItemsUpdatingScrollMode="KeepLastItemInView"
                       VerticalOptions="End"
                       Margin="8,8,8,0"
                       VerticalScrollBarVisibility="Never">
            <CollectionView.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding HideKeyboardCommand}" />
            </CollectionView.GestureRecognizers>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="entities:ChatMessage">
                    <Grid Padding="4,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Message -->
                        <SwipeView>
                            <SwipeView.RightItems>
                                <SwipeItems Mode="Execute">
                                    <SwipeItem BackgroundColor="Transparent"
                                               Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditMessageCommand}"
                                               CommandParameter="{Binding .}"
                                               Text="Edit" />
                                    <SwipeItem BackgroundColor="{AppThemeBinding Light=#EF4444, Dark=#B91C1C}"
                                               Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteMessageCommand}"
                                               CommandParameter="{Binding .}"
                                               Text="Delete"
                                               IconImageSource="trash" />
                                </SwipeItems>
                            </SwipeView.RightItems>
                            <Border x:Name="bubbleBorder"
                                    Grid.Column="0"
                                    BackgroundColor="{AppThemeBinding Light=#A0DAB2, Dark=#4D7B63}"
                                    HorizontalOptions="Start"
                                    Margin="8,6,80,6"
                                    StrokeShape="RoundRectangle 18"
                                    StrokeThickness="0"
                                    Shadow="{OnPlatform iOS={StaticResource SmallShadow}}"
                                    Padding="12,8">
                                <Border.Triggers>
                                    <!-- Settings for patient messages -->
                                    <DataTrigger TargetType="Border" Binding="{Binding SenderId}" Value="patient">
                                        <Setter Property="Grid.Column" Value="1" />
                                        <Setter Property="HorizontalOptions" Value="End" />
                                        <Setter Property="Margin" Value="80,6,8,6" />
                                        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#EAF4FC, Dark=#1E2A35}" />
                                    </DataTrigger>
                                </Border.Triggers>
                                <Border.Behaviors>
                                    <behaviors:VisualFeedbackBehavior />
                                </Border.Behaviors>
                                <Border.GestureRecognizers>
                                    <!-- Tap to reply -->
                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.HandleReplyToMessageCommand}"
                                                         CommandParameter="{Binding .}" />
                                </Border.GestureRecognizers>
                                <VerticalStackLayout Spacing="2">
                                    <!-- Reply Preview -->
                                    <Grid IsVisible="{Binding ReplyTo, Converter={StaticResource IsNotNullConverter}}"
                                          Margin="0,0,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="4" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        
                                        <!-- Вертикальная полоса, связывающая с оригинальным сообщением -->
                                        <BoxView Grid.Column="0" 
                                                 BackgroundColor="{AppThemeBinding Light=#2D6A4F, Dark=#8FD5A6}"
                                                 CornerRadius="2"
                                                 Margin="0,2,0,2"
                                                 HorizontalOptions="Start" />
                                         
                                        <Frame Grid.Column="1"
                                               Padding="6,3"
                                               CornerRadius="8"
                                               BackgroundColor="{AppThemeBinding Light=#A0DAB2, Dark=#4D7B63}"
                                               Opacity="0.85"
                                               BorderColor="Transparent"
                                               HasShadow="False">
                                            <Frame.Triggers>
                                                <DataTrigger TargetType="Frame" Binding="{Binding ReplyTo.SenderId}" Value="patient">
                                                    <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#EAF4FC, Dark=#1E2A35}" />
                                                </DataTrigger>
                                            </Frame.Triggers>
                                            <StackLayout Orientation="Vertical" Spacing="2">
                                                <StackLayout Orientation="Horizontal" Spacing="3">
                                                    <Image Source="{FontImageSource FontFamily=MaterialIcons, Glyph=&#xe15e;, Size=12, Color={AppThemeBinding Light=#2D6A4F, Dark=#FFFFFF}}"
                                                           VerticalOptions="Center"
                                                           Margin="0,0,0,0" />
                                                    <Label Text="{Binding ReplyTo.SenderId, StringFormat='Reply to {0}'}"
                                                           TextColor="{AppThemeBinding Light=#2D6A4F, Dark=#FFFFFF}"
                                                           FontSize="10"
                                                           FontAttributes="Bold"
                                                           VerticalOptions="Center">
                                                        <Label.Triggers>
                                                            <DataTrigger TargetType="Label"
                                                                       Binding="{Binding ReplyTo.SenderId}"
                                                                       Value="patient">
                                                                <Setter Property="Text" Value="Reply to you"/>
                                                            </DataTrigger>
                                                        </Label.Triggers>
                                                    </Label>
                                                </StackLayout>
                                                <Label Text="{Binding ReplyTo.Content}"
                                                       TextColor="{AppThemeBinding Light=#105E29, Dark=#FFFFFF}"
                                                       FontSize="11"
                                                       MaxLines="1"
                                                       LineBreakMode="TailTruncation"
                                                       Margin="0,0,0,0"/>
                                            </StackLayout>
                                        </Frame>
                                    </Grid>

                                    <!-- Text content -->
                                    <Label x:Name="MessageText"
                                           Text="{Binding Content}" 
                                           TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"
                                           FontSize="17"
                                           Margin="0,4,0,0"
                                           LineBreakMode="WordWrap"
                                           LineHeight="1.25"
                                           VerticalOptions="Center">
                                        <Label.Triggers>
                                            <!-- sender colour tweak -->
                                            <DataTrigger TargetType="Label" Binding="{Binding SenderId}" Value="doctor">
                                                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}" />
                                            </DataTrigger>
                                            <!-- hide text if this message is an image -->
                                            <DataTrigger TargetType="Label" Binding="{Binding Type}" Value="{x:Static entities:MessageType.Image}">
                                                <Setter Property="IsVisible" Value="False" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>

                                    <!-- Image content -->
                                    <Image x:Name="MessageImage"
                                           Source="{Binding LocalAttachmentPath}"
                                           Aspect="AspectFill"
                                           HeightRequest="200"
                                           IsVisible="False"
                                           Margin="0,4,0,0">
                                        <Image.Triggers>
                                            <DataTrigger TargetType="Image" Binding="{Binding Type}" Value="{x:Static entities:MessageType.Image}">
                                                <Setter Property="IsVisible" Value="True" />
                                            </DataTrigger>
                                        </Image.Triggers>
                                    </Image>

                                    <Label Text="{Binding Timestamp, StringFormat='{0:HH:mm}'}"
                                           TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"
                                           FontSize="11"
                                           Opacity="0.8"
                                           HorizontalOptions="End"/>
                                </VerticalStackLayout>
                            </Border>
                        </SwipeView>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Input Panel -->
        <Grid Grid.Row="1"
              RowDefinitions="Auto,Auto,Auto"
              BackgroundColor="{AppThemeBinding Light=#F7F7F7, Dark=#121212}">
            
            <!-- Reply Preview -->
            <Grid Grid.Row="0"
                  IsVisible="{Binding ReplyToMessage, Converter={StaticResource IsNotNullConverter}}"
                  Margin="8,4,8,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="4" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <!-- Вертикальная полоса слева -->
                <BoxView Grid.Column="0" 
                         BackgroundColor="{AppThemeBinding Light=#2D6A4F, Dark=#8FD5A6}"
                         CornerRadius="2"
                         Margin="2,4,2,4"
                         HorizontalOptions="Start" />
                
                <Frame Grid.Column="1"
                       BackgroundColor="{AppThemeBinding Light=#A0DAB2, Dark=#4D7B63}"
                       Opacity="0.85"
                       Padding="10,6"
                       CornerRadius="12"
                       x:Name="ReplyIndicator"
                       BorderColor="Transparent"
                       HasShadow="{OnPlatform iOS=True, Android=False}">
                    <Frame.Triggers>
                        <DataTrigger TargetType="Frame" Binding="{Binding ReplyToMessage.SenderId}" Value="patient">
                            <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#EAF4FC, Dark=#1E2A35}" />
                        </DataTrigger>
                    </Frame.Triggers>
                    <Grid ColumnDefinitions="*,Auto">
                        <VerticalStackLayout Spacing="2">
                            <StackLayout Orientation="Horizontal" Spacing="3">
                                <Image Source="{FontImageSource FontFamily=MaterialIcons, Glyph=&#xe15e;, Size=12, Color={AppThemeBinding Light=#2D6A4F, Dark=#FFFFFF}}"
                                       VerticalOptions="Center" />
                                <Label Text="Reply to doctor's message"
                                       TextColor="{AppThemeBinding Light=#2D6A4F, Dark=#FFFFFF}"
                                       FontAttributes="Bold"
                                       FontSize="11">
                                    <Label.Triggers>
                                        <DataTrigger TargetType="Label"
                                                   Binding="{Binding ReplyToMessage.SenderId}"
                                                   Value="patient">
                                            <Setter Property="Text" Value="Reply to your message"/>
                                        </DataTrigger>
                                        <DataTrigger TargetType="Label"
                                                   Binding="{Binding ReplyToMessage.SenderId}"
                                                   Value="doctor">
                                            <Setter Property="Text" Value="Reply to doctor's message"/>
                                        </DataTrigger>
                                    </Label.Triggers>
                                </Label>
                            </StackLayout>
                            <Label Text="{Binding ReplyToMessage.Content}"
                                   TextColor="{AppThemeBinding Light=#105E29, Dark=#FFFFFF}"
                                   FontSize="12"
                                   MaxLines="1"
                                   LineBreakMode="TailTruncation"
                                   Margin="0,1,0,0"/>
                        </VerticalStackLayout>
                        <Button Grid.Column="1"
                                Text="×"
                                Command="{Binding CancelReplyCommand}"
                                BackgroundColor="Transparent"
                                TextColor="{AppThemeBinding Light=#8E8E93, Dark=#636366}"
                                FontSize="20"
                                Padding="0"
                                Margin="-8,-8,-8,-8"
                                HeightRequest="36"
                                WidthRequest="36"
                                VerticalOptions="Center"
                                IsVisible="True"/>
                    </Grid>
                </Frame>
            </Grid>
            
            <!-- Edit Message Preview -->
            <Frame Grid.Row="1"
                   IsVisible="{Binding EditingMessage, Converter={StaticResource IsNotNullConverter}}"
                   BackgroundColor="{AppThemeBinding Light=#FEF9E7, Dark=#4D3C14}"
                   Margin="8,4,8,0"
                   Padding="12,8"
                   CornerRadius="12"
                   BorderColor="Transparent">
                <Grid ColumnDefinitions="*,Auto">
                    <VerticalStackLayout Spacing="2">
                        <Label Text="Editing message"
                               TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"
                               FontSize="12"/>
                    </VerticalStackLayout>
                    <Button Grid.Column="1"
                            Text="×"
                            Command="{Binding CancelReplyCommand}"
                            BackgroundColor="Transparent"
                            TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"
                            FontSize="20"
                            Padding="0"
                            Margin="-8,-8,-8,-8"
                            HeightRequest="36"
                            WidthRequest="36"
                            VerticalOptions="Center"/>
                </Grid>
            </Frame>

            <!-- Input Controls -->
            <Grid Grid.Row="2"
                  ColumnDefinitions="44,*,44"
                  Padding="8,6,8,8">
                <Button Grid.Column="0"
                       Text="＋"
                       BackgroundColor="Transparent"
                       TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"
                       Shadow="{OnPlatform iOS={StaticResource SmallShadow}}"
                       FontSize="24"
                       Padding="0"
                       Margin="0"
                       HeightRequest="38"
                       WidthRequest="38"
                       VerticalOptions="Start"
                       x:Name="AttachmentButton">
                    <Button.Behaviors>
                        <behaviors:MenuFlyoutBehavior x:Name="AttachmentMenu" />
                    </Button.Behaviors>
                </Button>

                <Frame Grid.Column="1"
                       BackgroundColor="{AppThemeBinding Light=#EAF4FC, Dark=#1E2A35}"
                       Padding="8,0"
                       CornerRadius="18"
                       HasShadow="True"
                       Shadow="{OnPlatform iOS={StaticResource SmallShadow}}"
                       BorderColor="Transparent"
                       Margin="4,0,4,0"
                       MinimumHeightRequest="38"
                       MaximumHeightRequest="120"
                       VerticalOptions="Start">
                    <Editor Text="{Binding MessageText}" 
                           Placeholder="Message"
                           PlaceholderColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}"
                           TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"
                           BackgroundColor="Transparent"
                           FontSize="17"
                           AutoSize="TextChanges"
                           MaxLength="1000"
                           Keyboard="Chat"
                           VerticalTextAlignment="Center"
                           Margin="0,-2,0,-2"/>
                </Frame>

                <Button Grid.Column="2"
                       IsVisible="{Binding MessageText, Converter={StaticResource StringNotEmptyConverter}}"
                       Text="↑"
                       Command="{Binding SendMessageCommand}"
                       BackgroundColor="Transparent"
                       TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"
                       Shadow="{OnPlatform iOS={StaticResource SmallShadow}}"
                       FontSize="24"
                       FontAttributes="Bold"
                       Padding="0"
                       Margin="0"
                       HeightRequest="38"
                       WidthRequest="38"
                       VerticalOptions="Start"/>
            </Grid>
        </Grid>
    </Grid>
</ContentPage> 